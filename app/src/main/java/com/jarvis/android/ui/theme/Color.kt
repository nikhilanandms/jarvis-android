package com.jarvis.android.ui.theme

import androidx.compose.ui.graphics.Color

// — Obsidian palette —
val ObsidianBg        = Color(0xFF080810)   // near-black, blue undertone
val ObsidianSurface   = Color(0xFF0E0E1A)   // barely lifted surface
val ObsidianVariant   = Color(0xFF15151F)   // card / input surface
val ObsidianBorder    = Color(0xFF22223A)   // subtle border

val Gold              = Color(0xFFC8A86A)   // warm amber gold — accent
val GoldDim           = Color(0xFF2B2010)   // user bubble background (very dark gold)
val GoldMuted         = Color(0xFF6B5830)   // secondary gold

val TextPrimary       = Color(0xFFEDE8DF)   // warm cream — not pure white
val TextSecondary     = Color(0xFF5C5A72)   // muted slate
val TextTertiary      = Color(0xFF2E2C3E)   // barely visible placeholder

val Positive          = Color(0xFF52D98A)   // success green
val Destructive       = Color(0xFFE05C5C)   // error red
